---
last_edited: 2026-06-25
---

# Tech Debt Tracker

This is the retrofit backlog required by the harness plugin setup.

| Class | Item | Current state | Next action |
| --- | --- | --- | --- |
| now | Agent-first directory scaffold | Added local router, standards, codemap, specialized docs, routed indexes, proof root, and check wrapper. Gate passed. | Keep `scripts/check` passing. |
| now | Worktree environment setup | Added skill-local setup script and environment config. Setup command creates `.codex-worktree/env.sh` and now defaults generated state, runtime root, and Cargo target under `.codex-worktree/` unless explicitly overridden. | Source `.codex-worktree/env.sh` before long-running validation in Codex-created worktrees. |
| now | Local ultragoal bundle gate | Added `LANE_REGISTRY.json`, `VERIFICATION_BACKLOG.json`, `COMPLETION_MANIFEST.json`, `AMENDMENTS.jsonl`, and `RED_FIXTURES.json`. The Rust-only gate enforces local bundle shape, digest freshness, strict repo-relative artifact paths, passing local validator receipt status, and key schema-required fields. It is an intentionally bounded local gate, not full plugin `ultragoal-audit` parity and not feature-completion proof. | Keep bundle files locally valid, keep full `ultragoal-audit` parity outside the claim ceiling unless a future contract explicitly restores it, and preserve the `CLAIM-008` through `CLAIM-013` claim ceiling until live evidence exists. |
| now | Clock/alignment negative fixtures | Added fixture coverage for malformed timestamps, missing timestamps, out-of-window events, duplicate transcript text, missing audio anchors, and wall-clock skew. | Prove live Record & Replay clock behavior, monotonic drift assumptions, and real metadata/event alignment. |
| now | Documentation freshness law | Added documentation freshness to the local standards and classified it as not-yet mechanized instead of pretending static lint can infer semantic doc staleness. | Add a Rust stale-doc blocker receipt validator once changed-file to doc-impact mapping is explicit. |
| blocked | Coverage proof | `scripts/check-coverage-full` runs real `cargo llvm-cov` line coverage and generated `validation_artifacts/coverage/coverage-receipt.json`. Current measured Rust line coverage is 96.26019705477276%, below the required 100% floor. | Add tests or remove uncovered repo-owned code until `scripts/check-coverage-full` reaches 100%; do not use generated/vendor/external exclusions for repo-owned Rust. |
| blocked | Coverage scope authority | Added `.harness/coverage-manifest.json`, `scripts/check-coverage-fast`, and `scripts/check-coverage-full`. Current manifest is Rust-source-only and records `DEBT-HU010-002` because scripts/docs/templates/product surfaces are not covered by true coverage tooling. | Extend mechanical coverage/proof scope or keep non-Rust coverage-complete claims withheld with explicit rationale. |
| blocked | Product Fitness | Added `PRODUCT_FITNESS.md`, `scripts/check-product-fitness`, Product Fitness standards rows, and a blocked receipt at `validation_artifacts/harness/product-fitness-receipt.json`. | Generate a real schema-valid Product Fitness receipt before product-fit, daily-driver, release, or material product signoff claims. |
| blocked | Fit-repo 0.0.10 receipt | Debt matrix records that no schema-valid Harness Ultragoal 0.0.10 fit-repo receipt exists in `validation_artifacts/harness/`. | Generate or validate a 0.0.10 fit-repo receipt after source gates and package sync are clean. |
| now | Redaction enforcement | Added initial generated-artifact redaction for obvious secret tokens, email-like identifiers, and sensitive key values, plus a redaction policy doc. | Add broader private transcript and broad-log negative fixtures, then inspect a real dogfood packet for leakage. |
| later | Runtime receipts | Setup, bundle, synthetic runtime fixture receipts, Record & Replay smoke proof, narrow coordinated narrated live transcription/drain proof, and a 2026-06-21 non-toy coordinated dogfood receipt exist under `validation_artifacts/`. The real dogfood receipt and refreshed review contract both report `requires-operator-review`; operator privacy/product-cohesion/usefulness review is still owed. | Review the 2026-06-21 generated artifacts without copying raw transcript text into durable docs, then add monotonic drift and live replay-engine proof. |
| withheld-claim | Review UI | Static local review artifact and CLI review command exist; they are report artifacts, not a complete UI/control surface. Browser/UI runtime proof, product-cohesion review, and recovery-state proof remain owed. | Run review against a real non-toy packet, then add browser/runtime and product-cohesion proof before making UI claims. |
| withheld-claim | Replay-time voice controls | Typed replay-time voice parameter planning artifact and CLI parameter validation exist; actual replay behavior is not implemented or demonstrated. | Connect typed voice parameters to a replay engine surface and add replay behavior proof before closing `CLAIM-013`. |
